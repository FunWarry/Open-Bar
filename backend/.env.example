# Exemple de format uniquement — ne pas utiliser cette valeur telle quelle.
# Générer un vrai secret aléatoire : export JWT_SECRET=$(openssl rand -base64 32)
JWT_SECRET=change-me-in-production-minimum-256-bits
