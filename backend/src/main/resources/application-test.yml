# Profile TEST — Base de données de test (Seeding automatique des 99 cocktails)
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:gestion_cocktail_test}?sslmode=disable
    username: ${DB_USER:postgres}
    password: ${TEST_DB_PASSWORD:${POSTGRES_PASSWORD:${DB_PASSWORD:KZiXrsAw/asRtE8YJuxA8LkWtDXK5rPBmiriAHAu+7Q=}}}

logging:
  level:
    org.springframework.security: INFO
    com.bar.gestioncocktail: INFO
    org.hibernate.SQL: off
