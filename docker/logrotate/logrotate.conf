# Global logrotate configuration for OpenBar log collector
# Rotate log files daily or when reaching size limits
daily

# Keep 14 days of backlogs
rotate 14

# Create new (empty) log files after rotating old ones
create

# Use gzip compression for rotated log files
compress
delaycompress

# Include additional logrotate definitions
include /etc/logrotate.d
