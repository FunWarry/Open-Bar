# OpenBar — Features State & Roadmap

> Last update: August 29, 2026 — PR #347 / Issue #336: GitHub Dependabot configuration for automated Maven, npm, and GitHub Actions dependency updates (.github/dependabot.yml), 100% green tests (SonarCloud Quality Gate PASSED).

## Features Table

| Feature | Backend | Frontend | Tests | Notes |
|---------|---------|----------|-------|-------|
| **Local HTTPS Reverse Proxy & TLS Certificates (#338)** | — | ✅ | ✅ | Complete TLS termination and Nginx reverse proxy setup in `frontend/nginx.conf` and `docker-compose.prod.yml`: ports 80 and 443 exposed, automatic 301 HTTP to HTTPS redirect, TLS 1.2/1.3 protocols, modern ciphers, `Permissions-Policy: camera=(self)` header unlocking camera access for QR scanning on mobile browsers (iOS Safari / Android Chrome) and PWA Service Worker registration on local Wi-Fi (`https://192.168.1.x` / `https://openbar.lan`), certificate generation scripts with Subject Alternative Names (`scripts/generate-local-certs.sh` & `scripts/generate-local-certs.ps1`), automatic bootstrap fallback self-signed cert (`frontend/entrypoint.sh`), validation test suite (`scripts/test-tls-reverse-proxy.js`). |
| **PostgreSQL Automated Backups & Retention (#337)** | ✅ | — | ✅ | Containerized scheduled backup service `backup` (`prodrigestivill/postgres-backup-local:15-alpine`) in `docker-compose.prod.yml`, daily cron schedule at 03:00 (`SCHEDULE: 0 3 * * *`), `.sql.gz` gzip compression, multi-tier retention policy (7 rolling days, 4 weekly backups, 6 monthly backups), persistent storage on dedicated Docker volume `openbar_backups`, manual CLI snapshot scripts (`scripts/backup-db.sh` & `scripts/backup-db.ps1`) and disaster recovery with pre-restore safety snapshot and lock clearing (`scripts/restore-db.sh` & `scripts/restore-db.ps1`), automated validation test suite (`scripts/test-backup-restore.js`). |
| **Dependabot Configuration (Maven, npm, GitHub Actions) (#336)** | ✅ | ✅ | ✅ | Full GitHub Dependabot configuration (`.github/dependabot.yml`): weekly updates for Maven (`/backend`) and npm (`/frontend`), monthly for GitHub Actions (`/`), targeting `dev` branch, conventional prefix `chore(deps):`, strict pin on Lombok (`org.projectlombok:lombok`) and JDK 22, grouped by ecosystem (Spring, Testcontainers, Angular, Ionic, Actions), CI SonarCloud validation integration. |
| **Log Centralization & Multi-Service Persistence (#335)** | ✅ | ✅ | ✅ | Shared persistent volume `openbar_logs` mounted across all services (`/var/log/openbar/...`), Logback configuration (`logback-spring.xml`) with rolling file appenders (10MB max, 14 days retention, 500MB cap) and non-blocking async appenders, structured Nginx frontend logs with latency metrics, PostgreSQL `logging_collector=on` with daily rotation, dedicated `logrotate` container (`docker/logrotate/`) with non-root `logrotate` user, unit tests `LoggingConfigurationTest` and Spring Boot integration tests `LoggingIntegrationTest`. |
| **Production Security Hardening & Strict CORS (#334)** | ✅ | — | ✅ | Systematic obfuscation of HTTP 500 error messages (`GlobalExceptionHandler`) in `prod` profile preventing technical/database schema leaks, dedicated `CorsOriginResolver` component filtering wildcard `*` in production for `SecurityConfig` and `WebSocketConfig` (endpoints `/ws` and `/api/ws`) with fallback on local subnets and allowed PWA domain, `OPENBAR_CORS_ALLOWED_ORIGINS` variable in `docker-compose.prod.yml`, `server.error.include-stacktrace: never` and `include-message: never`, unit tests `CorsOriginResolverTest`, `GlobalExceptionHandlerTest`, `SecurityConfigTest`, `WebSocketConfigTest` and Spring Boot integration suites `SecurityHardeningIntegrationTest` and `ProductionSecurityHardeningIntegrationTest`. |
| **Input Sanitization & Global XSS Protection (#333)** | ✅ | ✅ | ✅ | HTML/XSS sanitization engine `InputSanitizer` with Jsoup 1.18.3, automatic Jackson deserializer `SanitizingStringDeserializer`, `SanitizingDeserializerModifier` and `SanitizingRequestBodyAdvice` for all request DTOs, credential and sensitive key protection via `@NoSanitize`, reinforced Bean Validation constraints (`@NotBlank`, `@Size`, `@Email` in English), frontend utility `sanitizer.util.ts` and standalone pipe `SanitizePipe` (`appSanitize`), unit tests, non-regression and Testcontainers integration tests `SanitizationIntegrationTest`. |
| **Shift Presets & Weekly Schedule Revamp (#340)** | ✅ | ✅ | ✅ | Full demo schedule dataset in `demo_dataset.json`, revamped `ShiftPresetsConfigComponent` (`/manager/shift-presets`) with glassmorphic cards, dynamic calculation of net shift duration, quick break chips (15m, 30m, 45m, 60m) with haptic feedback, 4 live KPIs, 100% translatable Transloco FR/EN, Karma unit tests and Playwright E2E suite. |
| **Server Dashboard & Search Bar Modernization (#340)** | — | ✅ | ✅ | Streamlined server dashboard header (`DashboardServeurComponent`), removed redundant buttons, integrated visible table count badge in filters, custom `.search-input-box` search bar with animated icon, quick clear button and WCAG accessibility (hidden label), updated Karma tests. |
| **Floor Manager & Room Zones Overhaul (#340)** | — | ✅ | ✅ | Modernized `ZoneManagerComponent` with pill segment selector, interactive floor chips with counters, secure delete confirmation modal (`ConfirmDeleteModalComponent`), adaptive dark/light SCSS styling. |
| **Establishment Settings Page Unification (#340)** | ✅ | ✅ | ✅ | Unified legal settings, alerts/timers, currencies, and themes in `AppSettingsPageComponent` (`/admin/settings`), reactive tab navigation (`tab=timers`, `tab=currency`), unsaved changes guard (`PendingChangesGuard`), unit tests (22/22) and Playwright E2E. |
| **Configurable Establishment Currency & Dynamic Formatting (#326)** | ✅ | ✅ | ✅ | Backend entity `AppSettings`, columns `currency_code`, `currency_symbol`, `currency_position` (`BEFORE`/`AFTER`), endpoints `GET`/`PUT /api/settings`, standalone pipe `AppCurrencyPipe` (`appCurrency`), quick presets (EUR, USD, GBP, CHF, CAD, JPY, AUD), position setting with live receipt preview, dynamic formatting in PDFs (invoices, Z-reports) and across all 17 frontend components, Playwright E2E suite (`currency-settings.spec.ts`). |
| **Split Billing Payment Persistence & Dedicated Receipts (#330)** | ✅ | ✅ | ✅ | Entity `FactureReglement`, table `facture_reglements`, endpoints `POST /api/factures/{id}/split/encaisser` & `GET /api/factures/{id}/reglements`, persistence of partial settlements (equal splits and item selection), reactive recalculation and automatic invoice closure with table release, dedicated section in `FactureDetailComponent`, customized thermal receipt printing per guest via `TicketReceiptComponent`. |
| **Order WebSocket Notifications Deduplication (#330)** | — | ✅ | ✅ | Eliminated 9 duplicate notifications during server cart submission: removed redundant `/topic/barman/commandes` listener, `orderStatusMap` map in `NotificationService`, single notification emission upon creation and only on real status transitions (`EN_PREPARATION`, `PRET`, `LIVREE`, `ANNULEE`). |
| **Unread Notification Badge Sync & Mark All Read (#316)** | — | ✅ | ✅ | Reactive signal `unreadCount: Signal<number>` in `NotificationService` automatically synced on WebSocket receipts, `marquerLue`, and `marquerToutLu`. TopBar bell badge (`NavbarComponent`) 100% reactive (conditional display, `9+`, disappears on 0). Full Transloco translation of panel (`NotificationPanelComponent`), Karma unit test suite (1,499 tests) and Playwright E2E (`notification-sync.spec.ts`). |
| **Complete Dataset & Test Fixtures Overhaul (#315)** | ✅ | ✅ | ✅ | Comprehensive modernization of datasets (`cocktails_list.json` and `demo_dataset.json`), 20 detailed modular mixology recipes (`cocktail_recipe_steps`), robust backend seeders (`CocktailDataSeederService`, `SampleDataSeederService`, `GlasswareDataSeederService`) with `@DependsOn` ordering, order and invoice linkage (`FactureItem.commandeItem`), centralized frontend fixtures `frontend/src/test/fixtures/` (`mock-cocktails`, `mock-commandes`, `mock-factures`, `mock-glassware`, `mock-schedule`, `mock-tables`), aligned Playwright E2E mocks `mock-api.helper.ts`. 100% green tests and 0 SonarCloud defects. |
| **Variant Declaration with Dedicated Recipe Modal (#314)** | ✅ | ✅ | ✅ | Dedicated modal `VariantRecipeModalComponent` to configure name, price supplement, availability, notes, and step-by-step recipe customization (ingredients with `SearchableSelectComponent`, measurements, mixology action templates, free text). Quick portion multiplier. Automatic diff detection to keep "Standard recipe" status without DB duplication (`cocktail_variante_ingredients` and `recipe_steps_json` only if customized). Precise unit inventory deduction and dynamic display on `RecipeSidePanelComponent` (Bartender) and `VarianteModalComponent` (Server). 100% translatable Transloco FR/EN. |
| **Bartender Recipe Card & Modular Mixology Steps (#313)** | — | ✅ | ✅ | Enlarged right side panel (~760px), complete visual and typographic alignment with cocktail creation summary, numbered and colored badges per block type (green for ingredients, blue/indigo for mixology actions, orange for custom garnishes), dynamic display toggle ("Quick measurements" filtering ingredients vs "Detailed steps" showing full sequence), dynamic total measurements ($N \times \text{quantity}$) and per-glass units, visual glassware recommendation cards, interactive checkboxes to track step progress live, 100% translatable Transloco (strict FR/EN parity). |
| **Invoices Page, Detail & Printable Receipts Redesign (#323)** | — | ✅ | ✅ | Modern hero bar with Z-report shortcut, 4 live KPI cards (total revenue, invoice count, settlement rate, Z-report), advanced filters (multi-field search, payment methods, settlement status, date range, min/max amount), Grid / Table view toggle with column sorting, quick clipboard copy with feedback, glassmorphic A4 detail with establishment branding and metadata grid, realistic thermal receipt with 80mm/58mm formats, serrated edges, barcode, and print stylesheet (@media print), 100% translatable Transloco (strict FR/EN parity). |
| **Light Theme, Stepper Responsiveness & Creation Seasonality (#321)** | — | ✅ | ✅ | High-contrast light palette (`variables.css`, `input-field`, `searchable-select`), high-contrast white cards, `CocktailSaisonnaliteComponent` panel accessible from creation with state sync in submission payload, dynamic highlighting of active and current months, responsive adaptation of stepper header and ingredient cards. |
| **Multi-Screen Responsiveness & Adaptive Layout (#312)** | — | ✅ | ✅ | Main vertical scrolling container `.main-content` (`overflow-y: auto`), responsive navigation sidebar collapsing under 1200px via `NavigationService` and `window.matchMedia`, floating notification overlay panel with backdrop under 1400px, Bartender Kanban columns adaptable into 3 balanced tiers without mandatory horizontal scroll, protection against text overflow and badge truncation on order cards. |
| **Server Cart Order Submission & Real-Time Kanban Sync (#309)** | ✅ | ✅ | ✅ | Effective DB creation from server cart drawer (`createCommande` + `ajouterItem`), clean WebSocket broadcast `CommandeResponseDTO` on `/topic/commandes`, `/topic/commandes/statut`, and `/topic/barman/commandes` for all write operations (`create`, `update`, `delete`, `annuler`, `changerStatut`, `ajouterItem`, `retirerItem`), instant in-memory removal on cancellation (`ANNULEE`) in `DashboardBarmanComponent` and `KanbanServeurComponent`. |
| **Table Detail Modal & Active Order Modification Overhaul (#307)** | ✅ | ✅ | ✅ | Adaptive glassmorphic design (`TableDetailModalComponent`), table metadata (status, capacity, server, balance), active orders breakdown with contextual actions (Edit, Transfer, Cancel), interactive order edition modal (`EditCommandeModalComponent`) with live recalculation, auto stock release and STOMP broadcast (`/topic/commandes`, `/topic/barman/commandes`), glassware management (`Glassware`). |
| **Cocktail Builder via Modular Blocks & Repeatable Steps (#305)** | ✅ | ✅ | ✅ | 4-step wizard (`cocktail-form`), reorderable blocks (ingredients, mixology actions, custom text), action template library (`recipe_step_templates`), numbered sequential sheets and dynamic multi-portion calculation ($N \times \text{quantity}$) on Bartender `recipe-side-panel`. |
| **Mandatory Test Coverage & JaCoCo / Karma Rules (#281)** | ✅ | ✅ | ✅ | Comprehensive unit & non-regression test suite (1,321 frontend Karma tests, 555 backend JUnit 5 tests, 21 Playwright E2E tests), JaCoCo rule in pom.xml, Karma rule in karma.conf.js, SonarCloud Quality Gate PASSED. |
| **Integration Tests (Testcontainers) & E2E (Playwright) (#193)** | ✅ | ✅ | ✅ | Full test pyramid: Spring Boot Testcontainers (Auth, Commande, Stock, Facture, Non-Regression), Playwright E2E (Auth, Server/Bartender/Manager Dashboards, Cocktails, Ingredients, Billing, Client QR, Non-Regression), dedicated CI job. |
| **Full Translation & Codebase / UI Internationalization (#282)** | ✅ | ✅ | ✅ | 100% UI translatable via Transloco (2,034 FR/EN keys in strict parity), 100% JavaDoc/TSDoc/OpenAPI/tests/comments in English across backend and frontend |
| Full Documentation & OpenAPI/Swagger API (#192/#194) | ✅ | ✅ | ✅ | JavaDoc, TSDoc, OpenAPI 3.0 |
| SonarCloud Quality Gate & 100% Security without `@SuppressWarnings` | ✅ | ✅ | ✅ | Coverage > 80%, Grade A |
| **Centralized Demo Dataset & Dynamic Seeders (#297)** | ✅ | ✅ | ✅ | 100% centralized in `demo_dataset.json` (floors, zones, tables, users, 26 shifts, closures, orders, invoices), dynamic `SampleDataSeederService.java`, 0 hardcoded data |
| **Table Billing & Settlement (#295)** | ✅ | ✅ | ✅ | `GET /api/factures/table/{id}/addition`, `POST /api/factures/table/{id}/encaisser`, `EncaissementModalComponent`, Equal/Item-based split, change calculator, tips, discounts, 80mm receipt, A4 PDF, real-time STOMP |
| Staff Schedule & Shift Management (#275) | ✅ | ✅ | ✅ | `/employees` view, shift modal per employee, presets, closures, drag & drop duplicate, WS `/topic/schedule-publications` |
| Establishment Legal Data SIRET/TVA/RCS (#129/#134) | ✅ | ✅ | ✅ | SIRET Luhn validation, French TVA format, Admin Form + Live receipt preview |
| Multi-Rate VAT Calculation (20%, 10%, 5.5%) (#130) | ✅ | ✅ | ✅ | HT/VAT/TTC breakdown per item, CA3 tax summary |
| Sequential Invoice Numbering (FAC-YYYY-NNNNN) (#131) | ✅ | ✅ | ✅ | CGI Art. 289 compliance, Credit notes (AV-YYYY-NNNNN) |
| 10-Year Legal Archiving & SHA-256 Integrity (#132) | ✅ | ✅ | ✅ | Immutable finalized invoices + SHA-256 PDF hash verification |
| Accounting CSV Export & Monthly VAT Report (#133) | ✅ | ✅ | ✅ | UTF-8 BOM Excel export, monthly VAT summary |
| Thermal 80mm Receipt & A4 PDF Invoice (#180) | ✅ | ✅ | ✅ | Thermal 80mm ticket, legal header, VAT breakdown, payment terms |
| JWT Authentication | ✅ | ✅ | ✅ | — |
| Initial Setup Wizard (/setup admin) | ✅ | ✅ | ✅ | — |
| Figma UI Component Library | — | ✅ | ✅ | — |
| Common Screens (Login, Register, Profile, 404, Loading) | — | ✅ | ✅ | — |
| Bartender Screens (Kanban, Badges, Action buttons) | — | ✅ | ✅ | — |
| Profile Preferences & Notification Toggles (#234) | — | ✅ | ✅ | Figma 540:1022 PREFERENCES section (sound/visual toggles + language selector) |
| JWT Refresh Token | ✅ | ✅ | ✅ | Token rotation + HTTP interceptor |
| User Management (Admin) | ✅ | ✅ | ✅ | UserService REST + UserListComponent CRUD & UserDialog modal (#204/#203) |
| ADMIN/MANAGER/SERVEUR/BARMAN Roles | ✅ | ✅ | ✅ | — |
| Output DTOs (All Controllers) | ✅ | — | ✅ | Java records `from(entity)` |
| GlobalExceptionHandler | ✅ | — | ✅ | Centralized error handling |
| Frontend Error Interceptor | — | ✅ | ✅ | Toast and error feedback |
| Cocktail Allergen Filter (#244) | ✅ | ✅ | ✅ | Frontend multi-select allergens, combined with category filters |
| Global Language Selector (Logged Out) (#245) | ✅ | ✅ | ✅ | FR/EN toggle in NavbarComponent + floating glassmorphic button in AppComponent |
| Cocktails CRUD | ✅ | ✅ | ✅ | Full catalog management |
| Cocktail Seasonality | ✅ | ✅ | ✅ | Seasonal availability management |
| Variants & Automatic Inventory Deduction (#185/#182) | ✅ | ✅ | ✅ | Selection & customization modal |
| Ingredients CRUD & Routing (/ingredients) (#219) | ✅ | ✅ | ✅ | Endpoint `GET /api/ingredients`, `/ingredients` routes (+ new/detail/edit), modal & dark theme select |
| Ingredients Stock PATCH (#249) | ✅ | ✅ | ✅ | CORS PATCH allowed, `/{id}/stock` and `/{id}/seuil-alerte` endpoints accept PUT+PATCH + `@RequestParam` or JSON `@RequestBody` |
| Tables CRUD | ✅ | ✅ | ✅ | Full floor plan table management |
| Order Transfer Between Tables (#186/#205) | ✅ | ✅ | ✅ | Transfer button & TransfertModalComponent connected (#205/#207) |
| Orders Lifecycle | ✅ | ✅ | ✅ | Complete order flow |
| Public QR Code Ordering (#184) | ✅ | ✅ | ✅ | Customer mobile ordering interface |
| Auto Inventory Depletion (EN_PREPARATION & Variants) | ✅ | — | ✅ | Real-time stock decrement on status change |
| Stock Alerts via WebSocket | ✅ | ✅ | ✅ | Critical and warning threshold notifications |
| Real-time WebSocket Notifications (Toasts + Navbar panel) | ✅ | ✅ | ✅ | STOMP client event handling |
| STOMP Broadcast Service (#187) | ✅ | — | ✅ | Centralized topic publisher |
| Sound & Visual Notifications (#181) | ✅ | ✅ | ✅ | Web Audio API synthesizer & UI badges |
| Order & Stock Alert Threshold Configuration (#197) | ✅ | ✅ | ✅ | Manager and Bartender timer threshold settings |
| Invoices (List + Detail + Settlement) | ✅ | ✅ | ✅ | Billing and payment processing |
| Receipt Merging (#186) | ✅ | ✅ | ✅ | FusionModalComponent in floor plan |
| Invoice PDF Export | ✅ | ✅ | ✅ | OpenPDF A4 compliant with French billing regulations |
| Bill Splitting (Equal / Item Selection) | ✅ | ✅ | ✅ | Interactive split workflows |
| Manager Dashboard (#293) | ✅ | ✅ live STOMP + analytics | ✅ | Complete manager cockpit, WebSocket STOMP sync, 6 KPI cards, top cocktails, order flow, critical stock alerts, enhanced Kanban with filters, CSV export, 1,235 frontend + 469 backend tests 100% green, SonarCloud 87.0% coverage |
| Bartender Dashboard (#292) | ✅ | ✅ real-time kanban | ✅ | Real-time STOMP Kanban, dynamic preparation timers with sound and visual urgency alerts, expandable recipe cards, quick stock outage modal, 80mm thermal receipt printing, multi-criteria filters and search, adaptive theme, 1,224 frontend + 469 backend tests 100% green, SonarCloud Grade A (86.6% coverage, 0 bugs) |
| Server View (Floor plan + variants modal #182) | ✅ | ✅ modal + new order + kanban + 2D plan | ✅ | Complete 2D floor plan sync with /plan-salle (theme, shapes, centered timer badge, multi-zone selection, smooth view transitions, touch gesture isolation) |
| Interactive Konva.js Floor Plan (#291) | ✅ | ✅ | ✅ | Konva.js reactive canvas, 1px=1cm scale, edge-to-edge magnetic snapping & 50cm grid snap, non-edit mode protection, 0 lag, Figma compliant |
| Customer QR Code View (Order placement + STOMP tracking) | ✅ | ✅ | ✅ | Complete mobile customer view (`/client/commande`, `/client/suivi/:id`) |
| Configurable Timezone (Establishment / TimeService) | ✅ | ✅ | ✅ | Admin-configurable TimeZone + System fallback |
| System Audit Log (/api/audit-logs) (#206) | ✅ | ✅ | ✅ | Admin component & service |
| Global TopBar Aligned with Figma (#208) | — | ✅ | ✅ | NavbarComponent |
| Global NavBar / Sidebar 64-220px Figma (#209) | — | ✅ | ✅ | SidebarComponent |
| Design System Refactoring & Figma Atomic Components (#210) | — | ✅ | ✅ | UI Figma Atoms |
| Figma Application Views Harmonization (#211) | — | ✅ | ✅ | ProductCard, StatCard, etc. |
| **Immutable Schedule Audit Log + Time-Travel Replay (#285)** | ✅ | ✅ | ✅ | `shift_audit_log` table, `ShiftAuditService`, `ScheduleHistoryModalComponent`, `ShiftHistoryModalComponent`, schedule replay at time T, SonarCloud 81.3% |
| **Modern Server Order Taking (#290)** | ✅ | ✅ | ✅ | `NouvelleCommandeComponent`, live search, category/allergen filters, HT/VAT/TTC breakdown, 100% Karma tests (1,195/1,195), SonarCloud 100% coverage |

## Active Technical Debt

| # | Description | Status |
|---|-------------|--------|
| 1 | Spring `allow-circular-references: true` | ⚠️ To be refactored |
| 2 | Bug `dateLivraison` set on `PRET` instead of `LIVREE` | ⚠️ Known bug |
| 3 | 13 devDeps CVEs in Angular (esbuild, babel, vite) | ⚠️ Angular 22 required |

## Resolution History

| PR / Issue | Description |
|------------|-------------|
| #347 (#336) | GitHub Dependabot configuration (`.github/dependabot.yml`) for automated Maven (backend), npm (frontend), and GitHub Actions workflows dependency management: strict targeting of `dev` branch, conventional prefix `chore(deps):`, exclusion rules for Lombok (`org.projectlombok:lombok`) to maintain pinned JDK 22 compatibility, logical dependency grouping (Spring, Testcontainers, Angular, Ionic, GitHub Actions) to minimize PR noise, CI integration and SonarCloud Quality Gate PASSED. |
| #339 (#326) | Configurable establishment currency in manager settings and global dynamic formatting: full configuration of ISO code (e.g. USD, EUR, GBP), currency symbol (e.g. $, €, £, CHF), and symbol position (Before / After amount) via backend entity `AppSettings`, table `app_settings` (`currency_code`, `currency_symbol`, `currency_position`), `AppSettingsResponseDTO`, `AppSettingsUpdateRequest`, `AppSettingsService`. Reactive frontend service `AppSettingsService` with signals and getters, standalone Angular pipe `AppCurrencyPipe` (`appCurrency`) uniformly applied across all components (product cards, cash receipts, server cart drawer, checkout and payment modals, variant recipe sheets, invoice details and lists, customer order tracking), dynamic adaptation of price formatting in PDFs generated by `PdfService` (official A4 invoices and daily Z-reports). Modern visual configuration card in `OrderTimersSettingsComponent` with 1-click presets (EUR, USD, GBP, CHF, CAD, JPY, AUD), position toggle, and receipt simulator with real-time price preview. Strict Transloco FR/EN translation parity (`SETTINGS.CURRENCY_*`), 1,562 Karma frontend unit tests, 684 JUnit 5 backend tests, 37 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED. |
| #331 (#330) | Split billing payment persistence, custom thermal receipt printing per guest, and WebSocket notification deduplication: backend entity `FactureReglement`, table `facture_reglements`, endpoints `POST /api/factures/{id}/split/encaisser` and `GET /api/factures/{id}/reglements`, direct share association (equal or item-based with serialized JSON), reactive recalculation of remaining balance and automatic invoice finalization with table release (`FactureService`), full partial payment history display with status badges and reprinting in `FactureDetailComponent`, configurable 80mm/58mm thermal receipt generator and printing per guest (`TicketReceiptComponent`) with barcode `${facture.numero}-P${partIndex}` and HT/TTC/VAT breakdown. Eliminated 9 duplicate notifications on server cart order creation via `orderStatusMap` in `NotificationService`. 100% Transloco FR/EN parity, 1,543 Karma frontend tests, 674 JUnit 5 backend tests, 36 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED (80.4% new coverage, 0 bugs, 0 vulnerabilities, 0 technical debt). |
| #327 (#314) | Variant declaration with dedicated recipe customization modal (`VariantRecipeModalComponent`): complete modal with name configuration, price supplement, availability, instruction notes, quick portion multiplier buttons (0.5x, 1x, 1.5x, 2x, custom), fine step-by-step customization (ingredients with `SearchableSelectComponent`, mixology action templates, free text), smooth reordering with up/down arrows, automatic diff detection relative to base recipe to preserve "Standard recipe" status without duplicate DB records (`cocktail_variante_ingredients` and `recipe_steps_json` only if customized), backend persistence and precise unit depletion (`CocktailService`, `CommandeService`), dynamic resolution in `RecipeSidePanelComponent` (Bartender) and `VarianteModalComponent` (Server), full Transloco FR/EN parity, 1,491 Karma unit tests, 631 JUnit backend tests, 33 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED. |
| #325 (#313) | Complete overhaul of the bartender recipe card (`RecipeSidePanelComponent`): enlarged panel width (~760px), complete visual and typographic alignment with cocktail creation summary, numbered and colored badges per step type (green for ingredients with `leaf-outline`, blue/indigo for mixology actions with stopwatch and contextual icon, orange for custom finishes), dynamic display toggle ("Quick measurements" filtering ingredients vs "Detailed steps" displaying full sequence), dynamic total measurements ($N \times \text{quantity}$) and per-glass units, visual glassware recommendation cards, interactive checkboxes to track step progress live, dynamic resolution of nested ingredient and action names (`template.name`, `ingredientName`), 100% translatable Transloco FR/EN in strict parity, 1,463 Karma frontend tests, 639 JUnit 5 backend tests, 33 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED. |
| #324 (#323) | Complete design revamp of invoices page (/factures), invoice detail (/factures/:id), and printable thermal receipts (80mm/58mm): modern hero bar with Z-report shortcut, 4 live KPI cards (total revenue, invoice count, settlement rate, Z-report), advanced filters (multi-field search, payment methods, settlement status, date range, min/max amount), Grid / Table view toggle with column sorting, quick clipboard copy with feedback, glassmorphic A4 detail with establishment branding and metadata grid, realistic thermal receipt with 80mm/58mm formats, serrated edges, simulated barcode, and print stylesheet (@media print), 100% translatable Transloco (strict FR/EN parity), 1,437 Karma frontend tests, 639 JUnit 5 backend tests, 33 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED. |
| #318 (#312) | Global responsiveness, vertical scrolling, and column layout improvements across all application views: fixed root vertical scrolling container in `.main-content` to solve truncated views (especially `/manager/timers`), responsive event listener in `NavigationService` to automatically collapse sidebar under 1200px, notification drawer transformed into floating modal/overlay with blurred clickable backdrop under 1400px to avoid compressing central content, Bartender Kanban grid reconfigured to size all 3 columns at 33.3% without requiring horizontal scroll on medium/large screens and clean single-column fallback on mobile (<768px), flex-wrap and text-ellipsis protection on order card headers. 1,430 Karma frontend tests, 622 JUnit 5 backend tests, 33 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED. |
| #310 (#309) | Fixed order creation from server cart drawer (`cart-drawer` on `DashboardServeurComponent`): effective DB creation via `DashboardServeurService.createCommande()` then `ajouterItem()` for each item line, cart cleared after successful API validation, immediate real-time WebSocket sync on all backend mutation endpoints (`CommandeService.java`: create, update, delete, cancel, change status, add/remove item with auto-cancellation if 0 items) broadcasting clean `CommandeResponseDTO` (avoids Jackson JPA serialization errors), instant in-memory removal of cancelled orders without page reload in Bartender Kanban (`DashboardBarmanComponent`) and Server Kanban (`KanbanServeurComponent`), table counts and lists refreshed. 1,401 Karma frontend tests, 616 JUnit 5 backend tests, 23 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED (86.6% new coverage, 0 bugs, 0 code smells). |
| #308 (#307) | Complete revamp and modernization of table detail modal (`TableDetailModalComponent`) and live modification of active orders (`EditCommandeModalComponent` + `PUT /api/commandes/{id}/modifier`): rich table metadata (status, capacity, assigned server, active order balance), detailed breakdown of active orders with contextual actions per order (Edit, Transfer, Cancel with confirmation and automatic stock restoration), interactive edit modal with cocktail selector (`SearchableSelectComponent`), quantity adjustments (+/-), line item deletion, item-level notes and global instructions, tips, real-time total recalculation, and STOMP WebSocket sync (`/topic/commandes`, `/topic/barman/commandes`). Complete glassware management system (`Glassware`, `GlasswareController`, `GlasswareService`, built-in presets + custom uploads). Detail modal integration on table click on Bartender (`CommandeCardComponent`) and Server sides. 100% green tests (1,363 Karma frontend tests, 616 JUnit 5 backend tests, 23 Playwright E2E tests), SonarCloud Quality Gate PASSED (80.0% new coverage, 0 bugs, 0 code smells, 0 vulnerabilities). |
| #306 (#305) | Complete overhaul of the cocktail builder with ordered modular block system, reusable mixology action template library (Shake, Strain, Muddle, Stir, Ice, Top Up, Garnish, Flame), custom free-text steps, 4-step wizard with stepper (`cocktail-form`), live preview with interactive portion selector, and dynamic multi-glass adaptation ($N \times \text{quantity}$) on Bartender panel (`recipe-side-panel`). Endpoints `/api/recipe-step-templates`, 100% dynamic seeders in `demo_dataset.json`, Transloco FR/EN parity, 1,322 Karma frontend unit tests, 582 JUnit backend tests, 23 Playwright E2E tests, SonarCloud Quality Gate PASSED (87.4% new coverage, 0 bugs, 0 code smells). |
| #303 (#193) | OpenBar full test pyramid implementation: Spring Boot integration tests with Testcontainers (`BaseIntegrationTest`, `AuthIntegrationTest`, `CommandeIntegrationTest`, `StockIntegrationTest`, `FactureIntegrationTest`, `NonRegressionIntegrationTest`), Playwright End-to-End test suite (`login.spec.ts`, `serveur-commande.spec.ts`, `barman-kanban.spec.ts`, `cocktails-catalog.spec.ts`, `stock-management.spec.ts`, `reglement.spec.ts`, `schedule-employees.spec.ts`, `qr-client-order.spec.ts`, `non-regression.spec.ts`), deterministic Playwright API mock (`mock-api.helper.ts`), `data-testid` on all interactive components, CI `e2e` Playwright job in `.github/workflows/ci.yml`. 1,265 Karma + 496 backend + 21 Playwright E2E tests 100% green, SonarCloud Quality Gate PASSED (82.2% coverage). |
| #301 (#297) | Removal of all hardcoded demo/test data in code (Java and TypeScript) and 100% centralization in `backend/src/main/resources/data/demo_dataset.json` (floors, zones with polygon points/radii, 25 tables with dimensions/shapes/rotations/positions/assignments, users with roles, 26 staff shifts with breaks and scheduled hours, Sunday and holiday closures, 16 orders and 9 invoices). Refactoring of `SampleDataSeederService.java` with dynamic seeders `seedShiftsFromJson` and `seedClosuresFromJson` without static residue, cleaned hardcoded fallbacks in `dashboard-serveur.service.ts` and `plan-salle.component.ts`, neutralized zone grouping in `dashboard-serveur.component.ts`, 1,260 frontend + 482 backend tests 100% green, SonarCloud Quality Gate PASSED (82.9% new coverage, 0 bugs, 0 warnings, 0 hotspots). |
| #300 (#295) | Complete handling of table billing and settlement with delivered orders for servers and managers: `GET /api/factures/table/{tableId}/addition` (dynamic calculation, item details, 20% VAT, HT/TTC), `POST /api/factures/table/{tableId}/encaisser` (official invoice generation `FAC-YYYY-NNNNN`, `REGLEE` status, automatic table release, STOMP notifications `/topic/tables` and `/topic/commandes`, audit log entry), modern adaptive settlement modal (`EncaissementModalComponent`), single payment mode with interactive change calculator and quick tokens (+5€, +10€, +20€, +50€, exact amount), tip selection (+5%, +10%, custom), commercial discounts (% or fixed amount), Split mode (equal split with guest count management and unit settlements via `ReglementModalComponent`, or item-assignment split with reactive balance tracking), 80mm thermal receipt printing and A4 PDF download, entry points in `TableDetailModalComponent`, `KanbanServeurComponent` (Pay button in LIVREE) and `DashboardServeurComponent`, complete FR/EN translations `ENCAISSEMENT.*` and `FACTURE.MODE_*`, 1,257 frontend + 480 backend tests 100% green, SonarCloud Quality Gate PASSED. |
| #298 (#292) | Complete overhaul and modernization of bartender dashboard (`/barman`): real-time Kanban with `EN_ATTENTE`, `EN_PREPARATION` and `PRET` columns synchronized via STOMP WebSocket (`/topic/commandes`, `/topic/commandes/{id}`, `/topic/stock/alerte`), live preparation timers with color shifting (Normal ➔ Warning ➔ Pulsing Urgency `⚡ URGENT`) and Web Audio API alerts, full recipe cards with measurements and expandable instructions directly on each ticket without leaving workflow, quick outage management modal ("Quick Stock") to disable a cocktail or adjust an ingredient stock during service, 80mm bar preparation order printing, multi-criteria filters and live search, strict adherence to theme CSS variable system, complete FR/EN translations `BARMAN_DASHBOARD.*`, 1,224/1,224 Karma unit tests 100% green, SonarCloud Quality Gate PASSED (86.6% new coverage, 0 bugs, 0 warnings). |
| #297 (#291) | Full alignment of server 2D floor plan view (`/serveur`) with the floor plan editor (`/plan-salle`): visual synchronization (rectangles, circles, ovals, polygons, neon glow and borders according to status/occupancy), snap-to-grid (50cm) and table magnetic edge-snapping, centered waiting timer badge beneath capacity without rotational distortion, multi-zone chip filtering, edit side panel protection outside edit mode, fixed Konva canvas reset and floor filtering on display mode switch, disabled mobile pull-to-refresh (`[disabled]="displayMode === 'PLAN'"`) and blocked native touch gestures on canvas (`touch-action: none !important`), 469 backend + 1,212 frontend tests 100% green, production CI build OK. |
| #294 (#290) | Complete redesign and modernization of server order taking (`/serveur/nouvelle-commande/:tableId`): real-time text search, beverage category filter chips (Cocktails, Soft Drinks, Beers, Wines, Snacks), allergen exclusion selector (Lactose, Gluten, Peanuts...), reactive cart with quantity adjustment, accounting breakdown HT/VAT (20%)/Total TTC, 100% theme CSS variables, completed FR/EN i18n keys `SERVEUR.*`, 1,195/1,195 Karma tests 100% green, SonarCloud Quality Gate PASSED (100.0% new coverage, 0 bugs). |
| #289 (#285) | Immutable schedule audit log of shift modifications + historical view at instant T (time-travel replay): `shift_audit_log` table, `ShiftAuditLog`/`ShiftAuditAction` entities, `ShiftAuditService` (`logCreation`, `logUpdate`, `logDeletion`, `reconstructScheduleAt`, `getAuditLogForWeek`), endpoints `GET /api/shifts/{id}/history`, `GET /api/schedule/audit-log`, `GET /api/schedule/at`, modals `ScheduleHistoryModalComponent` (filter by action/employee, before/after snapshot, replay trigger) and `ShiftHistoryModalComponent` (individual shift history), Time-Travel Replay mode in `ScheduleComponent` (datetime picker, live diff vs current schedule, read-only banner), FR/EN i18n (`SHIFTS.AUDIT`, `SHIFTS.REPLAY`), 470 backend tests + 1,190 frontend tests, SonarCloud Quality Gate PASSED (81.3% new coverage, 0 bugs). |
| #287 (#276) | Fix schedule comparison button & shifts UX: `forkJoin` synchronization for schedule and publication loading (eliminates disappearance of comparison button on week change), clicking empty cell directly opens pre-filled creation form (`initialDate`, `openInCreateMode`), employee header converted to semantic `<button>` to open employee shift list, and 100% Karma tests / SonarCloud Quality Gate PASSED. |
| #279 (#275) | Team schedule & Shift management per employee (Figma 540:1022): Manager Employees View (`/employees`), per-person shift management modal (`EmployeeShiftModalComponent`), establishment closure modal (`ClosureConfigModalComponent`), day closure exception modal (`DayClosureModalComponent`), shift template configuration (`ShiftPresetsConfigComponent`), drag-and-drop duplication, keyboard shortcuts (Ctrl+C, Ctrl+V, Del, Esc), schedule publication with real-time STOMP notifications `/topic/schedule-publications`, and SonarCloud Quality Gate 80.8% OK. |
| #271 (#270) | Interactive and responsive order detail modal (Figma 630:1540): opens on card click, item breakdown with unit prices and line subtotals, built-in action buttons to advance status or cancel with AlertController confirmation, eliminated all duplication, and refactored 100% to theme CSS variables. CI & 1,059 Karma tests 100% green. |
| #253 (#228) | Individual Settlement View Post-Split (`FactureSplitComponent`, Figma `630:1264`): reactive balance card (Total, Settled, Remaining balance, progress bar), 'Settle this share' buttons with `ReglementModalComponent`, status badges per guest ('SETTLED' vs 'PENDING'), automatic global invoice and table finalization as soon as all shares are settled, 98/98 Karma tests passing. |
| #252 (#235) | Settlement and tip management modal (`ReglementModalComponent`, Figma `628:1068`): reactive tip buttons (+5%, +10%, custom), dynamic total recalculation, cash change return calculator, backend `pourboire` support in `reglerFacture`, FR/EN i18n, 95/95 Karma tests passing. |
| #251 (#239) | Fix Profile form pre-filling from NgRx Auth store: reactive subscription with `takeUntil(destroy$)`, save via `UserService.updateUser()`, dispatch `setCurrentUser`, Toast i18n notification, cleaned memory leaks and Angular/TypeScript warnings. |
| #250 (#249) | Fix CORS PATCH + JSON body support on `PATCH /api/ingredients/{id}/stock` and `/{id}/seuil-alerte`: added `"PATCH"` in `SecurityConfig.java`, `PUT+PATCH` mappings in `IngredientController`, fixed Transloco `{{param}}` syntax in `fr.json` / `en.json`, cleaned redundant `ALTER TABLE` in `schema.sql`, +8 `IngredientControllerTest` unit tests (Sonar coverage PASSED). |
| #248 (#247) | Fix setup loop on restart: replaced `DROP TABLE IF EXISTS ... CASCADE` with `CREATE TABLE IF NOT EXISTS` in `schema.sql` — preserves data between restarts. |
| #246 (#245) | Global translation button: added FR/EN toggle in `NavbarComponent` (logged in) and floating glassmorphic button in `AppComponent` (logged out). |
| #220 | Refactored and aligned modal dialogs `UserDialogComponent` and `IngredientFormComponent` with Figma (atomic components `app-input-field`, interactive role chips, removed double asterisks and superfluous card wrappers, dark theme style for alert boxes and `ion-select` popovers). Fixed HTTP 500 error on `PUT /api/ingredients/{id}` and `PUT /api/users/{id}` requests (preserved existing entities and `createdAt` field). |
| #219 | Routing and integration of ingredients management (`/ingredients`, `/ingredients/new`, `/ingredients/:id`, `/ingredients/:id/edit`) with `AuthGuard` and `RoleGuard(['ADMIN', 'MANAGER', 'BARMAN'])`. Backend endpoint `GET /api/ingredients` + `getAllIngredients()` service. Fixed `<ion-refresher> must be used inside ion-content` warning on all list views (`ingredient-list`, `cocktail-list`, `commande-list`, `table-list`). |
| #218 (#217) | Interactive Theme Customization and Automatic HSL Palette Generator: created Admin Color Customization Studio (`PersonnalisationComponent`), interactive color pickers for global CSS variables (`--primary`, `--bg-0`, `--surface-1`) and 4 roles (`--role-admin`, `--role-manager`, `--role-serveur`, `--role-barman`), automatic HSL generator in `color-utils.ts`, 5 theme presets (Figma, Cyberpunk, Emerald, Sunset, Indigo), live reactive dynamic preview panel, `ThemeService` / `localStorage` persistence, 18/18 Karma tests passing, SonarCloud Quality Gate PASSED. |
| #216 (#211) | Figma Interface Alignment — Application Views and Composite Components: created `ProductCardComponent` (`app-product-card` Figma `129:95`), integrated atomic components `app-filter-chip` and `app-product-card` into QR Code Client view (`ClientCommandeComponent`), refactored `StatCardComponent` (`199:189`) and `StockSeverityBadgeComponent` (`133:133`) with English TSDoc, `data-testid` attributes and Angular `@if` syntax, 24/24 Karma unit tests passing, SonarCloud Quality Gate PASSED. |
| #215 (#210) | UI Design System Refactoring & Figma Atomic Components (0:1): Global CSS tokens instantiation (`--bg-0`, `--surface-1`, `--primary`, etc.), creation and enrichment of 9 reusable atomic UI components (ToggleSwitch, CheckboxField, Toast, Avatar, StatusBadge, RoleBadge, ActionButton, FilterChip, InputField) with English TSDoc, data-testid, 23/23 Jasmine/Karma unit tests, SonarCloud Quality Gate PASSED. |
